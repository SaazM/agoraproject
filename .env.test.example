# Copy to .env.test.local (gitignored) to enable the signed-in Playwright suite.
# Use a throwaway student account on your Supabase project — never a real user's login.
E2E_EMAIL=
E2E_PASSWORD=

# Optional: override the port Playwright starts vite on (default 5299).
# E2E_PORT=5299
